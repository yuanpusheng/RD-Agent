# RD-Agent China A-share monitoring environment template
# Copy this file to .env.monitoring (or supply via docker compose) and fill in the values.

# --- Data persistence ---
# DuckDB database backing the monitoring loop (will be created if missing).
RDC_DUCKDB_PATH=/app/var/data/market.duckdb
# Optional data cache directory for downloaded pricing data.
RDC_DATA_CACHE_DIR=/app/var/cache

# --- Monitoring configuration files ---
# Point to your monitoring rule definitions (defaults to the bundled examples).
RDC_MONITOR_RULES_PATH=/app/examples/ashare_monitoring/monitor_rules.yaml
# Optional alert subscription overrides (YAML).
RDC_MONITOR_ALERT_SUBSCRIPTIONS_PATH=/app/examples/ashare_monitoring/alert_subscriptions.yaml

# --- Data provider credentials ---
# TuShare API token to enable higher quality pricing data (optional but recommended).
TUSHARE_TOKEN=
# AkShare normally operates without an API key; set AKSHARE_TOKEN if you run a proxied deployment.
AKSHARE_TOKEN=

# --- Alert delivery ---
RDC_MONITOR_ALERT_CHANNELS_ENABLED=feishu
# Configure the Feishu/Lark incoming webhook and optional signing secret.
RDC_MONITOR_ALERT_FEISHU_WEBHOOK=
RDC_MONITOR_ALERT_FEISHU_SECRET=
# Uncomment to enable additional channels supported by the dispatcher.
# RDC_MONITOR_ALERT_WECOM_WEBHOOK=
# RDC_MONITOR_ALERT_SLACK_WEBHOOK=
# RDC_MONITOR_ALERT_EMAIL_WEBHOOK=
# RDC_MONITOR_ALERT_EMAIL_SECRET=

# --- Scheduler runtime ---
# Universe (or comma separated symbols) to evaluate; leave empty to use the config default.
MONITOR_UNIVERSE=CSI300
# Optional watchlist file or CSV of symbols for the scheduler container/script.
MONITOR_WATCHLIST=
# Set to "true" to enable the intraday interval defined by RDC_MONITOR_INTRADAY_INTERVAL_MINUTES.
MONITOR_ENABLE_INTRADAY=false
# Forwarded port for the Streamlit dashboard when using docker compose.
MONITOR_DASHBOARD_PORT=19555

# --- LLM configuration (only needed if you use alert digests that query an LLM) ---
# OPENAI_API_KEY=
